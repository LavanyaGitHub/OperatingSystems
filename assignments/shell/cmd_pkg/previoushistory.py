# """ #Name:previoushistory(!x)
# #Description:It loads the commands from the history to run again when given the specified input number
# #parameters :file
# """
# import threading
# import os
# import sys
# command_history = []
# def previoushistory(cmd):
# #	print "in prev"
	# x1=[]
	# y=[]
	# x1=list(cmd)
	# del x1[0]
	# files=''.join(x1)
	# files=eval(files)
	# new=list(enumerate(command_history))	
	# y=new[files]
	# newcmd= y[1].strip(" ")
	# runshell(newcmd)